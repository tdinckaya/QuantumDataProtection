<Project>
  <PropertyGroup>
    <!-- Allow building older TFMs with newer SDK when exact ref packs aren't available -->
    <TargetLatestRuntimePatch>false</TargetLatestRuntimePatch>
  </PropertyGroup>
</Project>
